# Домашнее задание к занятию «Репликация и масштабирование. Часть 2» Голоха Е.В.


### Задание 1

Опишите основные преимущества использования масштабирования методами:

- активный master-сервер и пассивный репликационный slave-сервер; 
- master-сервер и несколько slave-серверов;
- активный сервер со специальным механизмом репликации — distributed replicated block device (DRBD);
- SAN-кластер.

*Дайте ответ в свободной форме.*

### Решение 1

1. Активный master и пассивный slave
Это когда один сервер (master) работает и на нём всё происходит, а второй (slave) просто копирует с него данные.
```
Плюсы:

Есть запасной сервер, если первый сломается.

Можно читать данные со второго сервера, чтобы не нагружать основной.

Легко настроить и понять.
```

2. Один master и много slave'ов
Это как первый вариант, но с несколькими копиями slave-серверов.

``` Плюсы:

Можно читать данные с разных серверов — быстрее обрабатываются запросы.

Если один slave упал — есть другие.

Можно использовать разные серверы для разных задач: один — для отчётов, другой — для резервного копирования и т.д.
```
3. Сервер с DRBD
DRBD — это такая технология, которая копирует данные с одного сервера на другой на уровне жёстких дисков (а не просто базы данных).
```
Плюсы:

Очень надёжно — данные копируются сразу, почти одновременно.

Подходит для "железных" серверов с высокой надёжностью.

Часто используется в кластерах, где два сервера работают как одно целое.
```
4. SAN-кластер
SAN — это как большой быстрый внешний жёсткий диск, к которому могут подключаться несколько серверов.

```
Плюсы:

Несколько серверов могут работать с одним и тем же хранилищем данных.

Очень быстро и удобно.

Можно добавить больше памяти или серверов без остановки работы.

Хорошо подходит для больших проектов и баз данных.
```

### Задание 2


Разработайте план для выполнения горизонтального и вертикального шаринга базы данных. База данных состоит из трёх таблиц: 

- пользователи, 
- книги, 
- магазины (столбцы произвольно). 

Опишите принципы построения системы и их разграничение или разбивку между базами данных.

*Пришлите блоксхему, где и что будет располагаться. Опишите, в каких режимах будут работать сервера.* 

### Решение 2

![2025-06-23_09-16-00](https://github.com/user-attachments/assets/47028b3b-45c1-4840-ab07-056f8725b9b7)

ВЕРТИКАЛЬНЫЙ ШАРДИНГ (Vertical Sharding)

Суть:
```
Разделение данных по таблицам. Каждая таблица хранится в отдельной базе данных.

🔹 Что показано:
users_db — база данных только для таблицы пользователи.

books_db — база для книг.

shops_db — база для магазинов.

✅ Преимущества:
Каждая часть данных изолирована и может масштабироваться отдельно.

Можно использовать разные сервера, настройки, даже СУБД под разные типы данных.

Хорошо подходит, если таблицы слабо связаны.
```

ГОРИЗОНТАЛЬНЫЙ ШАРДИНГ (Horizontal Sharding)
```
Суть:
Разделение одной и той же таблицы на части, по какому-то признаку — например, по user_id, genre, region.

🔹 Что показано:
users_db_1 — хранит пользователей (например, книги с жанрами А до М)

users_db_2 — хранит пользователей (например, книги с жанрами от М до Я).

books_db_1 — книги с жанрами

users_db - пользователи взячвшие книги в Москве

✅ Преимущества:
Таблица становится "распараллеленной" — можно обрабатывать большие объемы данных быстрее.

Уменьшается нагрузка на каждый сервер.

Удобно при большом количестве записей в одной таблице (например, миллионы пользователей или книг).
```


